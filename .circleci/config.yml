
version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Greeting
          command: git tag ${CIRCLE_SHA1}

      - run:
          name: Print the Current Time
          command: date