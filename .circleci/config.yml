
version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: Install git
          command: apt-get update && apt-get install -y git

      - run:
          name: Greeting
          command: git tag ${CIRCLE_SHA1}

      - run:
          name: Git push
          command: git push --tags ${CIRCLE_SHA1}

      - run:
          name: Print the Current Time
          command: date